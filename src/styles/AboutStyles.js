import { makeStyles } from '@material-ui/core';
import '@fontsource/roboto';

export const AboutStyles = makeStyles({
    root: {
        color: 'white',
        height: '100vh',
    },
    author: {
        fontSize: '2rem',
        fontWeight: 'bold'
    },
    positions: {
        fontSize: '1.1rem',
    },
    gridHeight: {
        height: '100vh',
    }
});
